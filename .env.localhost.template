# SASS-E Local Deployment Configuration
# Copy this file to .env and fill in your values

# ============================================
# DATABASE CONFIGURATION
# ============================================
DB_ROOT_PASSWORD=your_secure_root_password
DB_NAME=sass_e
DB_USER=sass_e_user
DB_PASSWORD=your_secure_database_password

# ============================================
# APPLICATION CONFIGURATION
# ============================================
JWT_SECRET=your_very_long_random_secret_key_at_least_32_characters

# App Branding
VITE_APP_TITLE=SASS-E
VITE_APP_LOGO=/logo.svg

# Owner/Admin Configuration
OWNER_OPEN_ID=admin
OWNER_NAME=Administrator

# ============================================
# LLM API CONFIGURATION
# ============================================
# Option 1: OpenAI
LLM_API_URL=https://api.openai.com/v1
LLM_API_KEY=sk-your-openai-api-key-here

# Option 2: Anthropic Claude
# LLM_API_URL=https://api.anthropic.com/v1
# LLM_API_KEY=sk-ant-your-anthropic-key-here

# Option 3: Local LLM (Ollama)
# LLM_API_URL=http://localhost:11434/v1
# LLM_API_KEY=ollama

# ============================================
# OAUTH / AUTHENTICATION
# ============================================
# For localhost, we'll use a simplified auth system
# In production, replace with Auth0, Keycloak, or similar
OAUTH_SERVER_URL=http://localhost:3000/api/auth
VITE_OAUTH_PORTAL_URL=http://localhost:3000/login
VITE_APP_ID=sass-e-localhost

# ============================================
# STORAGE CONFIGURATION
# ============================================
# Option 1: Local filesystem (default for localhost)
# Leave empty to use local uploads folder

# Option 2: MinIO (self-hosted S3-compatible storage)
# S3_ENDPOINT=http://localhost:9000
# S3_ACCESS_KEY=minioadmin
# S3_SECRET_KEY=minioadmin
# S3_BUCKET=sass-e-uploads

# Option 3: AWS S3 or compatible service
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY=your_aws_access_key
# S3_SECRET_KEY=your_aws_secret_key
# S3_BUCKET=your-bucket-name

# ============================================
# OPTIONAL SERVICES
# ============================================
# Analytics (leave empty to disable)
ANALYTICS_ENDPOINT=
ANALYTICS_WEBSITE_ID=

# Tavily API (for web search features)
# TAVILY_API_KEY=tvly-your-key-here

# OpenWeather API (for weather features)
# OPENWEATHER_API_KEY=your-openweather-key

# ============================================
# NOTES
# ============================================
# 1. Generate JWT_SECRET with: openssl rand -base64 32
# 2. For LLM features, you MUST provide a valid API key
# 3. Storage will use local filesystem if S3 is not configured
# 4. OAuth is simplified for localhost - use proper OAuth in production
