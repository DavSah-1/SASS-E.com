import type { Express } from "express";

// OAuth callback handler removed - using Supabase Auth instead
// Authentication is now handled entirely on the frontend with Supabase client
export function registerOAuthRoutes(app: Express) {
  // No routes to register - Supabase handles authentication
}
